# Render a subset of markdown for the terminal.

Render a subset of markdown for the terminal.

## Usage

``` r
.render_markdown(text)
```

## Arguments

  - text:
    
    Character scalar (assistant response).

## Value

Character scalar with fenced code highlighted + light styling.
