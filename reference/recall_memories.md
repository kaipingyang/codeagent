# Recall memories as a compact block for system-reminder injection

Recall memories as a compact block for system-reminder injection

## Usage

``` r
recall_memories(max_chars = 2000L)
```

## Arguments

- max_chars:

  Integer. Cap total injected text.

## Value

Character(1). Empty string if no memories.
