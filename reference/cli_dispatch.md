# CLI dispatch helpers

Pure functions for resolving permission mode and dispatching CLI
arguments. Kept separate from exec/codeagent.R so they can be
unit-tested without running the full CLI entry point.
