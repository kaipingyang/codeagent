# Lint and format tools (lintr + styler)

codeagent-authored tools that let the agent statically analyse (lintr)
and auto-format (styler) the R code it writes, and a lint-based
`verify_fn` for the agent loop's verification step.
