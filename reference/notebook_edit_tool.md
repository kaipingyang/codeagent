# Create the NotebookEdit tool

Create the NotebookEdit tool

## Usage

``` r
notebook_edit_tool(mode = "default", rules = list(), ask_fn = NULL)
```

## Arguments

  - mode:
    
    Character. Permission mode.

  - rules:
    
    List. Permission rules.

  - ask\_fn:
    
    Function or NULL.

## Value

An `ellmer::tool()` object.
