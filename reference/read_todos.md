# Read the current todo list for a session

Read the current todo list for a session

## Usage

``` r
read_todos(session_id = "default")
```

## Arguments

  - session\_id:
    
    Character. Session id.

## Value

Character(1) markdown, or "" if no todo file exists.
