# Allow a tool call

Allow a tool call

## Usage

``` r
PermissionResultAllow(updated_input = NULL)
```

## Arguments

  - updated\_input:
    
    List or NULL. Modified tool input (if any).

## Value

Object of class `PermissionResultAllow`.
