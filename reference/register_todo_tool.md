# Register the TodoWrite tool on a chat

Register the TodoWrite tool on a chat

## Usage

``` r
register_todo_tool(chat, session_id = "default")
```

## Arguments

  - chat:
    
    An `ellmer::Chat` object.

  - session\_id:
    
    Character. Session id for the todo file.

## Value

Invisibly `chat`.
