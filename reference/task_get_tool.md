# Create the TaskGet tool

Create the TaskGet tool

## Usage

``` r
task_get_tool(store)
```

## Arguments

  - store:
    
    Environment. Per-session task store from `.new_task_store()`.

## Value

An `ellmer::tool()` object.
