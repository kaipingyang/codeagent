# Create the ExitPlanMode tool

Create the ExitPlanMode tool

## Usage

``` r
exit_plan_mode_tool(mode_env)
```

## Arguments

- mode_env:

  Environment with a `$mode` slot (the live permission mode).

## Value

An [`ellmer::tool()`](https://ellmer.tidyverse.org/reference/tool.html)
object.
