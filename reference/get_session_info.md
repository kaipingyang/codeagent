# Get metadata for a single session

Get metadata for a single session

## Usage

``` r
get_session_info(session_id, directory = NULL)
```

## Arguments

  - session\_id:
    
    Character. UUID.

  - directory:
    
    Character or NULL. Project directory.

## Value

A `SessionInfo` object, or `NULL` if not found.
