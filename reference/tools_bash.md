# Bash Tool

Execute shell commands with permission gating and optional sandboxing.
Shared helpers (`.tool_result`, `.make_permission_checker`) live in
`tools_builtin.R` and are available to all tool files.
