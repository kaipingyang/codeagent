% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_agent.R
\name{.run_subagent_loop}
\alias{.run_subagent_loop}
\title{Run a sub-agent's conversation loop, optionally persisting its session}
\usage{
.run_subagent_loop(
  sub_chat,
  prompt,
  max_turns = 30L,
  persist = FALSE,
  cwd = getwd(),
  description = NULL
)
}
\arguments{
\item{sub_chat}{An \code{ellmer::Chat} for the sub-agent.}

\item{prompt}{Character. The task prompt.}

\item{max_turns}{Integer. Max turns (currently single-shot chat).}

\item{persist}{Logical. Save the sub-agent session to disk.}

\item{cwd}{Character. Project dir for session storage.}

\item{description}{Character. Used as the sidechain session title.}
}
\value{
Character. The sub-agent's text response.
}
\description{
When \code{persist = TRUE} the sub-agent's full conversation is saved to a
"sidechain" JSONL under the project's session directory (id prefixed with
\verb{subagent-}), so sub-agent history survives instead of being ephemeral.
}
\keyword{internal}
