% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_dashboard.R
\name{team_dashboard}
\alias{team_dashboard}
\title{Live team-board dashboard}
\usage{
team_dashboard(db_path, poll_ms = 1500L, title = "codeagent — team board", ...)
}
\arguments{
\item{db_path}{Character. Path to the board SQLite file to monitor.}

\item{poll_ms}{Integer. Poll interval (ms) used only when the \code{watcher}
package is unavailable (event-driven otherwise). Default 1500.}

\item{title}{Character. Dashboard title.}

\item{...}{Passed to \code{\link[shiny:shinyApp]{shiny::shinyApp()}} \code{options} (e.g. \code{port}).}
}
\value{
A \code{shiny.appobj}.
}
\description{
A standalone Shiny app that live-monitors a shared task board
(\code{\link[=board_create]{board_create()}} / \code{\link[=team_coordinate]{team_coordinate()}}): task table (coloured by status),
a progress bar, and the inter-agent message log. Point it at the \code{db_path}
of a team running in another process (or a background \code{team_coordinate()}).

Updates are event-driven via \code{\link[=board_watch]{board_watch()}} (the \code{watcher} package) so the
view refreshes the instant the board changes; it falls back to periodic
polling when watcher is unavailable.
}
\keyword{internal}
