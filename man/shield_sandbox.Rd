% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{shield_sandbox}
\alias{shield_sandbox}
\title{Configure portable sandbox policy}
\usage{
shield_sandbox(
  project_root = getwd(),
  protected_paths = character(),
  temp_root = NULL,
  modes = list(project = "rwx", protected_data = "rw", temp = "rwx"),
  process_exec = TRUE,
  network = c("tool_policy", "deny"),
  symlink_escape = "deny",
  backend = c("auto", "policy", "required"),
  on_unavailable = c("policy", "block")
)
}
\arguments{
\item{project_root}{Project root (default current working directory).}

\item{protected_paths}{Additional protected data roots.}

\item{temp_root}{Session-specific temporary root; NULL creates one.}

\item{modes}{Named list using \code{r}, \code{rw}, or \code{rwx} for project,
protected_data, and temp.}

\item{process_exec}{Preserve exec-capability tools (default TRUE).}

\item{network}{\code{"tool_policy"} or \code{"deny"}.}

\item{symlink_escape}{Currently only \code{"deny"}.}

\item{backend}{\code{"policy"}, \code{"auto"}, or \code{"required"}.}

\item{on_unavailable}{\code{"policy"} fallback or \code{"block"} exec/net.}
}
\value{
A Data Shield sandbox strategy specification.
}
\description{
Restrict explicit tool path arguments to project/protected/session-temp roots
while preserving project \code{rwx} and process execution by default. This is a
portable policy guard, not a kernel sandbox. \code{backend="auto"} currently falls
back to policy because no full out-of-process OS adapter is implemented;
\code{on_unavailable="block"} can fail closed for exec/net tools.
}
