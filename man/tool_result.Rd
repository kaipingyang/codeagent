% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_display.R
\name{tool_result}
\alias{tool_result}
\title{Build a versioned cross-UI tool result}
\usage{
tool_result(
  value,
  kind = c("text", "table", "image", "code", "diff", "error"),
  payload = list(),
  title = NULL,
  icon = NULL,
  status = c("success", "error"),
  markdown = NULL
)
}
\arguments{
\item{value}{Character(1). Text summary returned to the model.}

\item{kind}{One of \code{"text"}, \code{"table"}, \code{"image"}, \code{"code"}, \code{"diff"},
\code{"error"}.}

\item{payload}{Named list holding the artifact, keyed by \code{kind}:
\itemize{
\item \code{table}: \verb{list(df = <data.frame>)}
\item \code{image}: \verb{list(images = list(list(mime = "image/png", b64 = <string>)), output = <text>)}
\item \code{code} : \verb{list(text = <code>, lang = , filename = , output = )}
\item \code{error}: \code{list(message = , detail = )}
\item \code{text} : \code{list(text = )}
}}

\item{title, icon}{Optional card title / icon name.}

\item{status}{One of \code{"success"}, \code{"error"} (forced to \code{"error"} when
\code{kind = "error"}).}

\item{markdown}{Optional markdown string attached to the display.}
}
\value{
An \link[ellmer:ContentToolResult]{ellmer::ContentToolResult}; return it from your tool function.
}
\description{
Construct an \link[ellmer:ContentToolResult]{ellmer::ContentToolResult} with three independent channels:
\code{value} is portable model-facing text; \code{extra$codeagent$artifact} is the
versioned, UI-neutral structured result; and \code{extra$display} is shinychat's
official presentation adapter. Return the result from your tool function.

Non-shinychat hosts should read the artifact with
\code{\link[=tool_result_artifact]{tool_result_artifact()}}, render supported \code{kind}/\code{payload} combinations
natively, and use \code{\link[=tool_result_value]{tool_result_value()}} as the fallback. They never need to
parse shinychat HTML. The streaming \code{on_tool_result} event exposes the same
\code{artifact}, \code{display}, and \code{value} channels.
}
\examples{
\dontrun{
summarise <- ellmer::tool(
  function(data_name) {
    df <- summary_frame(data_name)
    tool_result(sprintf("\%d x \%d summary", nrow(df), ncol(df)),
                kind = "table", payload = list(df = df),
                title = "Summary")
  },
  name = "Summarise", description = "...", arguments = list(...))
chat$register_tool(summarise)
register_tool_meta("Summarise", "read")
}
}
\seealso{
\code{\link[=codeagent_stream]{codeagent_stream()}}, \code{\link[=register_tool_meta]{register_tool_meta()}}
}
