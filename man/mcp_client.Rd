% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_client.R
\name{mcp_client}
\alias{mcp_client}
\title{MCP Client (connect to external MCP servers)}
\description{
Registers tools exposed by external Model Context Protocol
servers onto a codeagent Chat, via \code{mcptools::mcp_tools()}. This is the
client side (consuming external tools); \code{codeagent_mcp_server()} is the
server side (exposing codeagent's tools).

Transport: mcptools launches stdio MCP servers as child processes
(\code{command} + \code{args} + \code{env} per the config). HTTP/SSE servers depend on
mcptools support.

Config format (JSON file or inline list), e.g.:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "filesystem": \{
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"]
    \}
  \}
\}
}\if{html}{\out{</div>}}
}
\keyword{internal}
