% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_display.R
\name{render_artifact}
\alias{render_artifact}
\title{Render an artifact (typed tool-result data source) into an htmltools tag}
\usage{
render_artifact(artifact, mode = c("panel", "bubble"))
}
\arguments{
\item{artifact}{The structured data source: \code{list(kind, status, icon, title, payload)} from \code{extra$codeagent$artifact}. (Also tolerates being handed a
whole \code{display} list carrying a legacy \verb{$toolcard}, for backward-compat.)}

\item{mode}{\code{"bubble"} (compact, in-chat) or \code{"panel"} (full, right Output).
\code{bubble} drops the card header and chrome that the surrounding shinychat
tool card already provides, keeping only the copy action; \code{panel} renders
the full standalone card for the Output workspace. Both share the same
per-kind renderers and the same official code-block surface.}
}
\value{
An htmltools tag.
}
\description{
Branches on \code{artifact$kind} (code/image/table/diff/text/error). This is the
single renderer both the in-chat bubble and the right Output panel use.
}
\keyword{internal}
