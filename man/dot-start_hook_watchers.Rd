% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks_watch.R
\name{.start_hook_watchers}
\alias{.start_hook_watchers}
\title{Start filesystem-watch hooks for a session.}
\usage{
.start_hook_watchers(hooks, cwd = getwd(), config_paths = NULL, latency = 1)
}
\arguments{
\item{hooks}{A \link{HookRegistry} or NULL. NULL / no watcher package -> no-op.}

\item{cwd}{Directory to watch recursively for \code{FileChanged}.}

\item{config_paths}{Character vector of settings-file paths to watch for
\code{ConfigChange} (defaults to user + project settings.json).}

\item{latency}{Numeric seconds; watcher debounce (default 1).}
}
\value{
A list with a \verb{$stop()} method (idempotent) and the watcher objects,
or NULL if nothing could be started. Store it and call \verb{$stop()} on session
teardown.
}
\description{
Start filesystem-watch hooks for a session.
}
\keyword{internal}
