% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_board.R
\name{board_watch}
\alias{board_watch}
\title{Watch a task board for changes (event-driven coordinator engine)}
\usage{
board_watch(db_path, callback, latency = 0.3)
}
\arguments{
\item{db_path}{Character. Board path.}

\item{callback}{Function of one argument (changed paths).}

\item{latency}{Numeric. Debounce seconds (default 0.3).}
}
\value{
A started \code{watcher} R6 object, or \code{NULL} if watcher is unavailable.
}
\description{
Wraps \code{\link[watcher:watcher]{watcher::watcher()}} on the board file so a coordinator / live Shiny
view reacts to board changes the instant they land, instead of polling.
\code{callback} is invoked (with the changed paths) on every write to the board.
Returns the started watcher (call \verb{$stop()} when done), or \code{NULL} when the
watcher package is unavailable -- callers then fall back to polling (mirrors
how Shiny uses watcher when present and polls otherwise).
}
