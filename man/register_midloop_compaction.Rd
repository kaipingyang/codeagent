% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compaction.R
\name{register_midloop_compaction}
\alias{register_midloop_compaction}
\title{Register mid-loop compaction on a Chat (Plan B)}
\usage{
register_midloop_compaction(chat, settings = list())
}
\arguments{
\item{chat}{An \code{ellmer::Chat} object.}

\item{settings}{Named list from \code{\link[=load_settings]{load_settings()}}.}
}
\value{
Invisibly \code{chat}.
}
\description{
Adds an \code{on_tool_result} callback that compacts between tool rounds when over
threshold. Default = budget-aware micro snip (cheap, no LLM); opt in to a full
two-level compact mid-loop with \code{settings$midloop_full_compact}. The whole
feature is gated by \code{settings$midloop_compact} /
\code{options(codeagent.midloop_compact = TRUE)} (on by default via settings).
}
\keyword{internal}
