% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_gate.R
\name{tools_gate}
\alias{tools_gate}
\title{Unified tool permission gate}
\description{
A single central gate registered via \code{chat$on_tool_request()} that
governs EVERY tool call (codeagent-native, btw, Format, MCP) uniformly by
tool name -- mirroring Claude Code's central tool-execution pipeline.

ellmer already supports a rejectable central hook: \code{invoke_tools()} runs
\code{maybe_on_tool_request(request, cb)} which is
\verb{tryCatch(\{cb(request); NULL\}, ellmer_tool_reject = \\(e) ContentToolResult(error=...))};
a non-NULL result makes the loop skip the tool (\code{next}). The async loop does
\code{coro::await(cb(request))} inside the same tryCatch, so a promise-returning
callback can gate the Shiny path too. This gate therefore replaces the old
per-tool embedded checkers (tools built with \code{mode = "bypass"}).
}
\keyword{internal}
