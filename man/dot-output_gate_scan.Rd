% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output_gate.R
\name{.output_gate_scan}
\alias{.output_gate_scan}
\title{Run the Data Shield output gate on the model's final reply.}
\usage{
.output_gate_scan(text, settings = list(), chat = NULL, on_progress = NULL)
}
\arguments{
\item{text}{Character scalar. The model's final reply.}

\item{settings}{The agent settings list (\code{data_shield_engine},
\code{data_shield_response_on_fail}, \code{data_shield_output_scanners}).}

\item{chat}{The ellmer Chat (fallback shield source via attribute).}

\item{on_progress}{Optional progress callback forwarded to \code{scan_response()}.}
}
\value{
A list: \code{action} (\code{"pass"}/\code{"redact"}/\code{"block"}), \code{text} (the
possibly-redacted reply), and \code{matches} (count). When no shield is active,
returns \code{list(action = "pass", text = text)}.
}
\description{
Call this AFTER the model's reply is finalized, BEFORE returning it to the
user (CLI) or as a post-stream check (Shiny). No-op when no shield is active.
}
\keyword{internal}
