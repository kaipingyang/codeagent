% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team.R
\name{team_run}
\alias{team_run}
\title{Run a set of independent tasks as a parallel agent team}
\usage{
team_run(
  tasks,
  model = NULL,
  n_workers = NULL,
  permission_mode = "dont_ask",
  cwd = getwd(),
  parent_rules = NULL,
  parent_policy = NULL,
  security_context = NULL
)
}
\arguments{
\item{tasks}{Character vector of task prompts (one sub-agent per task).}

\item{model}{Character. Model spec each agent uses. Defaults to the
\code{CODEAGENT_MODEL} env var or \code{"claude-sonnet-4-6"}.}

\item{n_workers}{Integer or NULL. Number of parallel daemons. Defaults to
\code{min(length(tasks), parallelly::availableCores())} so it never exceeds the
container's cgroup CPU quota (each daemon is a heavy R process).}

\item{permission_mode}{Character. Permission mode for each agent (default
\code{"dont_ask"} since parallel agents cannot prompt interactively -- NOT
"bypass", so user-defined deny rules are still honoured).}

\item{cwd}{Character. Working directory for each agent.}

\item{parent_rules}{List. Permission rules inherited from the parent agent.}

\item{parent_policy}{List. Tool capability policies inherited from parent.}

\item{security_context}{Internal immutable parent security snapshot. When
supplied it takes precedence over the legacy permission arguments.}
}
\value{
A list (same length/order as \code{tasks}), each element either the
agent's text result or an \verb{[Error] ...} string.
}
\description{
Run a set of independent tasks as a parallel agent team
}
