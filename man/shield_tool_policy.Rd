% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{shield_tool_policy}
\alias{shield_tool_policy}
\title{Configure per-tool/agent Data Shield policy}
\usage{
shield_tool_policy(default = "scan", rules = list())
}
\arguments{
\item{default}{Default mode (\code{"scan"}).}

\item{rules}{Named list keyed by exact/glob tool names. Each rule may contain
\code{execution}, \code{ingress}, and \code{egress} set to \code{scan}, \code{bypass}, or \code{deny}.}
}
\value{
A Data Shield tool-policy strategy specification.
}
\description{
Override Shield handling by exact tool name or \code{*} glob. \code{scan} is the safe
default; \code{bypass} explicitly skips one Shield edge and is audited; \code{deny}
prevents execution (or blocks egress). Shield bypass never bypasses the
independent permission gate.
}
\examples{
trusted_plot <- shield_tool_policy(rules = list(
  KMPlot = list(ingress = "scan", egress = "bypass"),
  DangerousExport = list(execution = "deny")
))
}
