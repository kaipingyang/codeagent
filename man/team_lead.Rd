% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_lead.R
\name{team_lead}
\alias{team_lead}
\title{LLM-lead autonomous coordinator}
\usage{
team_lead(
  goal,
  model = NULL,
  cwd = getwd(),
  max_rounds = 3L,
  n_workers = NULL,
  permission_mode = "bypass",
  worktree = FALSE,
  decompose_fn = NULL,
  review_fn = NULL,
  coordinate_fn = NULL
)
}
\arguments{
\item{goal}{Character(1). The high-level objective.}

\item{model}{Character. Model spec for the lead and the workers.}

\item{cwd}{Character. Working directory.}

\item{max_rounds}{Integer. Maximum decompose/review rounds (default 3).}

\item{n_workers, permission_mode, worktree}{Passed to \code{\link[=team_coordinate]{team_coordinate()}}.}

\item{decompose_fn, review_fn, coordinate_fn}{Injectable steps (for testing);
default to ellmer structured calls + the real board.}
}
\value{
A data.frame: every task run across all rounds (with a \code{round} column).
}
\description{
A bounded "lead" loop that faithfully ports Claude Code's
COORDINATOR_MODE: a lead model decomposes a high-level goal into a task DAG,
a work-stealing team (\code{\link[=team_coordinate]{team_coordinate()}}) auto-claims and runs it, then the
lead reviews the results and either declares the goal done or adds a
follow-up round -- repeating up to \code{max_rounds}.

The three LLM/execution steps are injectable (\code{decompose_fn}, \code{review_fn},
\code{coordinate_fn}) so the loop control is unit-testable without a live model;
the defaults are ellmer structured-output calls + the real board.
}
\keyword{internal}
