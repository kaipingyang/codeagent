% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_gate.R
\name{data_shield_ocr_scanner}
\alias{data_shield_ocr_scanner}
\title{Build an OCR-backed image scanner for the Data Shield input gate.}
\usage{
data_shield_ocr_scanner(shield, on_fail = c("block", "pass"), engine = NULL)
}
\arguments{
\item{shield}{A \code{DataShield} engine (its \code{scan_prompt()} backs the scan).}

\item{on_fail}{How an OCR hit is handled: \code{"block"} (default --- immutable
image cannot be redacted in place, so the whole turn is blocked) or
\code{"pass"} (audit-only; log but let it through).}

\item{engine}{Optional pre-built \code{tesseract::tesseract()} engine (language,
whitelist, etc.). \code{NULL} uses tesseract's default English engine.}
}
\value{
A scanner function returning \code{list(action = "block"/"pass", text=)}.
}
\description{
Returns a \verb{function(content_image) -> list(action, ...)} suitable for the
\code{image_scanner} slot of \code{\link[=.input_gate_scan]{.input_gate_scan()}} (or \code{settings$data_shield_image_scanner}).
It OCRs the image with the optional \pkg{tesseract} package, then runs the
extracted text through the shield's \code{scan_prompt()}. Images are otherwise a
blind spot at edge 1 (the model sees pixels, not the redactable text inside
them); this closes the gap for text baked into screenshots.
}
\details{
This is \strong{opt-in} (scheme A): the default \code{data_shield_image_scanner} stays
\code{NULL}, so the host must wire this in explicitly. Reasons: OCR is a real
per-image cost, it can false-positive, and \pkg{tesseract} is a \code{Suggests}
dependency (a C library + language data, not installed by default). When
\pkg{tesseract} is missing the scanner degrades to \code{pass} (never blocks on a
missing optional dep) --- so the image is treated as an accepted blind spot,
matching the no-scanner default.
}
