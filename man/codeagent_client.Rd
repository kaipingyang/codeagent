% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{codeagent_client}
\alias{codeagent_client}
\title{Create a codeagent client from any ellmer Chat}
\usage{
codeagent_client(
  chat = NULL,
  permission_mode = "default",
  rules = list(),
  cwd = getwd(),
  max_turns = 100L,
  btw_groups = NULL,
  worktree_isolation = FALSE,
  verify_fn = NULL,
  mcp_config = NULL,
  register_tools = TRUE,
  data_shield = NULL,
  max_budget_usd = NULL
)
}
\arguments{
\item{chat}{An \code{ellmer::Chat} object -- any backend supported by ellmer:
\code{chat_openai_compatible()}, \code{chat_anthropic()}, \code{chat_ollama()}, etc.
If NULL, a chat is auto-built from \code{CODEAGENT_BASE_URL}/\code{CODEAGENT_MODEL}
env vars (or Anthropic defaults).}

\item{permission_mode}{Character. One of \link{PermissionMode}.}

\item{rules}{List of \code{\link[=PermissionRule]{PermissionRule()}} objects.}

\item{cwd}{Character. Working directory (used for CLAUDE.md, skills, sessions).}

\item{max_turns}{Integer. Maximum agentic loop turns.}

\item{btw_groups}{Character vector or NULL. btw tool groups to register
(e.g. \code{c("docs","git","pkg")}). NULL = all available groups.}

\item{worktree_isolation}{Logical. Run sub-agents in isolated git worktrees.}

\item{verify_fn}{Function or NULL. Optional output verifier; re-enters the
loop when it reports failures (e.g. \code{\link[=verify_r_tests]{verify_r_tests()}}).}

\item{mcp_config}{MCP client config (JSON path or inline list) to connect
external MCP servers; see \code{\link[=register_mcp_client]{register_mcp_client()}}. NULL disables.}

\item{register_tools}{Logical. If \code{TRUE} (default) register all tools now.
\code{FALSE} returns a lightweight shell (chat + settings + system prompt, no
tools) so callers (e.g. \code{\link[=codeagent_app]{codeagent_app()}}) can render UI first and defer the
expensive tool registration; call \code{\link[=.register_all_tools]{.register_all_tools()}} later.}

\item{data_shield}{\code{NULL} (off), a strategy list from \verb{shield_*()} (creates a
private \link{DataShield} R6), or an explicit \link{DataShield} instance shared by
selected chat threads. For a harness-only client, attach tools then call
\code{client$data_shield$install(client$chat)}.}

\item{max_budget_usd}{Numeric or NULL (default). Hard dollar-cost cap for
this client's \code{chat} (mirrors Claude Code's \code{maxBudgetUsd}), checked
alongside the token budget in \code{\link[=agent_loop]{agent_loop()}} via \code{chat$get_cost()}. NULL
(default) means no cap. Only takes effect where ellmer has price data for
the provider/model; unpriced custom endpoints (e.g. a Databricks/Azure
serving endpoint ellmer doesn't recognize) report cost \verb{$0} forever, so
the cap silently never fires there -- this is a known limitation, not a
bug (see \code{CODEAGENT_MAX_BUDGET_USD} env var / \code{max_budget_usd} in
settings.json for the same knob without a client-code change).}
}
\value{
Object of class \code{CodeagentClient} with \verb{$chat}, \verb{$settings}, and
\verb{$data_shield} (NULL when disabled).
}
\description{
Injects codeagent tools (Bash, Read, Write, Edit, Glob, Grep, LS, btw tools,
skill tool) and rebuilds the system prompt. The returned \code{CodeagentClient}
is the single object passed to \code{\link[=codeagent]{codeagent()}} and \code{\link[=codeagent_app]{codeagent_app()}}.
}
