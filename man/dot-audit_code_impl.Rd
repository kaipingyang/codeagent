% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_audit.R
\name{.audit_code_impl}
\alias{.audit_code_impl}
\title{Audit R code for risky external references (deterministic pipeline).}
\usage{
.audit_code_impl(
  code,
  shield = NULL,
  project_root = getwd(),
  max_bytes = 100000L
)
}
\arguments{
\item{code}{Character. The R code to audit (untrusted; parsed, not run).}

\item{shield}{Optional \code{DataShield} whose reviewer reviews the referenced
file contents. \code{NULL} -> report references only (no content review).}

\item{project_root}{Directory the whitelist confines reads to.}

\item{max_bytes}{Per-file read cap (avoid feeding huge files to the reviewer).}
}
\value{
A list: \code{static_paths}, \code{dynamic}, \code{allowed} (paths read),
\code{blocked} (list of path+reason not read), \code{reviews} (per-file reviewer
verdicts when a shield is given), \code{risk} (overall: "none"/"review"/"block").
}
\description{
Runs the full first-pass audit: AST extraction -> tool-layer path whitelist
-> deterministic read of whitelisted source files -> (optionally) feed the
read text to the Data Shield reviewer. The reviewer NEVER gets a
read/write/shell tool: this function decides what to read (code, not LLM),
bounds it to whitelisted in-project source files, and only hands the reviewer
vetted text.
}
\keyword{internal}
