% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{codeagent_app}
\alias{codeagent_app}
\title{Launch the codeagent Shiny application}
\usage{
codeagent_app(
  client = NULL,
  theme = "default",
  pinned_skills = character(0),
  greeting = NULL,
  port = NULL,
  launch.browser = TRUE,
  file_tree_show_hidden = FALSE,
  file_tree_exclude = c("renv", "node_modules", "packrat", ".git", ".Rproj.user"),
  chat_submit_key = c("enter", "enter+modifier"),
  model = NULL,
  permission_mode = "default",
  cwd = getwd(),
  btw_groups = NULL,
  chat = NULL
)
}
\arguments{
\item{client}{A \code{CodeagentClient} from \code{\link[=codeagent_client]{codeagent_client()}}, an
\code{ellmer::Chat}, or NULL (legacy mode).}

\item{theme}{UI theme. One of \code{"default"} (light Bootstrap 5), \code{"flatly"},
\code{"darkly"} (dark), or \code{"glass"} (dark glassmorphism). The CLI aliases
\code{"light"} -> \code{"default"}, \code{"dark"} -> \code{"darkly"}, and \code{"glassmorphism"} ->
\code{"glass"} are also accepted. Set at launch; the live dark-mode toggle in the
sidebar still flips light/dark on top of the chosen theme.}

\item{pinned_skills}{Character vector. Retained for backward compatibility;
the old Skills picker panel was replaced by the slash-command typeahead
(type \code{/} in the chat input), so this argument is currently unused.}

\item{greeting}{Character or NULL. If provided, pre-fills the chat input box
with this text on startup (used by the "Chat about selection" IDE addin to
seed the first message with the selected code). NULL leaves the input empty.}

\item{port}{Integer or NULL. Shiny port (NULL = random).}

\item{launch.browser}{Logical. Open in browser (default TRUE).}

\item{file_tree_show_hidden}{Logical. Show hidden dotfiles (e.g. \code{.git},
\code{.codegraph}) in the file tree. Default \code{FALSE} to reduce clutter/lag.}

\item{file_tree_exclude}{Character vector. Directory names excluded from the
file tree (default \code{renv}, \code{node_modules}, \code{packrat}, \code{.git},
\code{.Rproj.user}). Set \code{character(0)} to disable exclusion.}

\item{chat_submit_key}{How the chat input submits: \code{"enter"} (default, Enter
sends, Shift/Ctrl+Enter inserts a newline) or \code{"enter+modifier"}
(Ctrl/Cmd+Enter sends, plain Enter inserts a newline -- friendlier for
long multi-line prompts). Set at launch; not switchable live.}

\item{model}{Character. Legacy: model name.}

\item{permission_mode}{Character. Legacy: permission mode.}

\item{cwd}{Character. Legacy: working directory.}

\item{btw_groups}{Character vector or NULL. Legacy: btw tool groups.}

\item{chat}{An \code{ellmer::Chat}. Legacy alias.}
}
\value{
A \code{shiny.appobj}.
}
\description{
Launch the codeagent Shiny application
}
