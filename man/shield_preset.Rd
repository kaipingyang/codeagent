% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{shield_preset}
\alias{shield_preset}
\alias{shield_preset_strict}
\alias{shield_preset_balanced}
\alias{shield_preset_clinical}
\title{Ready-made Data Shield strategy combinations}
\usage{
shield_preset_strict()

shield_preset_balanced()

shield_preset_clinical()
}
\value{
A \code{list()} of Data Shield strategy specifications.
}
\description{
Three combination templates lifted verbatim from the "ready-to-use
combination templates" section of \code{vignette("data-shield")}, as callable
functions instead of copy-pasted code. Each returns a \code{list()} of strategy
specifications suitable for \code{codeagent_client(data_shield = ...)} or
\code{DataShield$new(strategies = ...)}. None of them register \code{shield_reviewer}
with a live client_factory except \code{shield_preset_clinical()}, which uses
\code{CODEAGENT_FAST_MODEL} like the rest of the package.
\itemize{
\item \code{shield_preset_strict()}: compliance/audit demos. Fails closed
(\code{on_fail = "block"} everywhere) and strict k-anonymity.
\item \code{shield_preset_balanced()}: everyday development, low friction. Redacts
instead of blocking; no ingress scanning or describe metadata.
\item \code{shield_preset_clinical()}: adds the semantic \code{shield_reviewer()} rail on
top of strict k-anonymity, with \code{"ask"} (not \code{"block"}) fail policy.
}

These are starting points, not a certification -- read the vignette's
"Combination safety" matrix before deploying any of them as-is.
}
\examples{
strict <- shield_preset_strict()
balanced <- shield_preset_balanced()
}
