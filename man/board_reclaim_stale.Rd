% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_board.R
\name{board_reclaim_stale}
\alias{board_reclaim_stale}
\title{Reclaim tasks whose worker died mid-flight}
\usage{
board_reclaim_stale(db_path, timeout = 300)
}
\arguments{
\item{db_path}{Character. Board path.}

\item{timeout}{Numeric. Seconds a \code{claimed} task may be held before it is
considered stale (default 300).}
}
\value{
Integer. Number of tasks reclaimed.
}
\description{
Resets \code{claimed} tasks that have been held longer than \code{timeout} seconds back
to \code{pending} (clearing owner + claimed_at) so another worker can pick them up.
This is the crash-recovery half of the coordinator: a worker that dies after
claiming a task would otherwise block its dependents forever. Called from the
worker loop's idle branch, so recovery happens without a separate lead.
}
