% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keyring.R
\name{keyring_integration}
\alias{keyring_integration}
\title{Keyring Integration (Optional)}
\description{
Optional helpers for storing API keys in the OS credential
store via the \code{keyring} package. When keyring is unavailable or the
secret service daemon is not running, all functions fall back gracefully
to \verb{~/.Renviron} (the existing behaviour).

Call hierarchy:
\itemize{
\item \code{keyring_store_key()} — save a key (keyring preferred, .Renviron fallback)
\item \code{keyring_get_key()} — retrieve a key (keyring → env var → "")
\item \code{.keyring_available()} — runtime probe; cached per session
}
}
\keyword{internal}
