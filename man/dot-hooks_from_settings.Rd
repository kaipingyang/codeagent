% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks.R
\name{.hooks_from_settings}
\alias{.hooks_from_settings}
\title{Build a HookRegistry from a settings.json \code{hooks} block}
\usage{
.hooks_from_settings(settings)
}
\arguments{
\item{settings}{List from \code{\link[=load_settings]{load_settings()}} (uses \code{settings$hooks}).}
}
\value{
A \link{HookRegistry}, or NULL if no valid hooks are declared.
}
\description{
Parses a declarative hooks specification (as loaded from settings.json) into
a live \link{HookRegistry}. The expected shape mirrors Claude Code:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{"hooks": \{
  "PreToolUse":  [\{ "command": "echo pre  >> /tmp/hooks.log" \}],
  "PostToolUse": [\{ "command": "echo post >> /tmp/hooks.log", "pattern": "Bash" \}]
\}
}\if{html}{\out{</div>}}

Each entry's \code{command} is run via the shell when the event fires; \code{pattern}
(optional) limits a tool hook to matching tool names.
}
\keyword{internal}
