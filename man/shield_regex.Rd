% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{shield_regex}
\alias{shield_regex}
\title{Configure regex-based egress scanning}
\usage{
shield_regex(
  patterns = NULL,
  include_defaults = TRUE,
  replacement = "[REDACTED]",
  on_fail = c("redact", "block"),
  ignore_case = TRUE
)
}
\arguments{
\item{patterns}{Optional named character vector of custom regular expressions
using PCRE (Perl-Compatible Regular Expression) syntax. Example:
\code{c(study_id = "STUDY-[0-9]+")}, where \code{study_id} names the rule and
\verb{[0-9]+} means one or more digits. With \code{include_defaults = TRUE}, custom
rules are appended after the built-ins.}

\item{include_defaults}{Include built-in email, phone-like, common-token-prefix,
and 18-character identity-number rules.}

\item{replacement}{Marker inserted once per merged matching span when
\code{on_fail = "redact"}.}

\item{on_fail}{\code{"redact"} replaces only matching spans and preserves the rest
of the tool result; \code{"block"} replaces the entire model-facing result.}

\item{ignore_case}{Apply case-insensitive PCRE matching to all rules.}
}
\value{
A Data Shield scanner strategy specification.
}
\description{
Detect and redact/block common unregistered PII and secrets in tool output.
Default rules cover email, phone-like numbers, common API-token prefixes,
and 18-character identity-number shapes. Custom named PCRE patterns may be
added without introducing another state engine.
}
\examples{
pii <- shield_regex(on_fail = "redact")
study_ids <- shield_regex(
  patterns = c(study_id = "STUDY-[0-9]+"),
  include_defaults = FALSE,
  on_fail = "block"
)
}
