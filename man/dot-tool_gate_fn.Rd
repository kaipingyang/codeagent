% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_gate.R
\name{.tool_gate_fn}
\alias{.tool_gate_fn}
\title{Install the central permission gate on a Chat}
\usage{
.tool_gate_fn(
  policy,
  resolve_mode,
  rules = list(),
  ask_fn = NULL,
  hooks = NULL
)
}
\arguments{
\item{rules}{List of fine-grained permission rules.}

\item{ask_fn}{\verb{function(name, input)} returning logical or promise\if{html}{\out{<logical>}},
or NULL (then \code{"ask"} becomes deny).}

\item{hooks}{A \code{HookRegistry} or NULL (fires PreToolUse/PostToolUse/PermissionDenied).}

\item{chat}{An \code{ellmer::Chat}.}

\item{settings}{Named list (for \code{settings$tools} policy).}

\item{mode_env}{Environment with \verb{$mode} (live permission mode) or a string.}
}
\value{
Invisibly \code{chat}.
}
\description{
Registers one \code{on_tool_request} callback that gates every tool by name, plus
an \code{on_tool_result} callback for PostToolUse hooks. Works for the sync
(\verb{$chat()}) and async (\verb{$chat_async()}/Shiny) paths: when the decision is
\code{"ask"} and \code{ask_fn} returns a promise, the gate returns a promise that the
async loop awaits (UI approval); a logical \code{ask_fn} is handled inline.
}
\keyword{internal}
