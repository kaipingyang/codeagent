% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks_watch.R
\name{hooks_watch}
\alias{hooks_watch}
\title{Filesystem-watch hooks (FileChanged / ConfigChange)}
\description{
Bridges the \code{watcher} package (libfswatch / inotify) to the
\code{FileChanged} and \code{ConfigChange} hook events. The watcher runs in the
background and dispatches callbacks through the \code{later} event loop, so it
works wherever a \code{later} loop is being pumped: the Shiny app (its reactive
loop) and the CLI REPL (which pumps \code{later} while streaming and, since the
non-blocking keypress change, while idle at the prompt too).

\code{watcher}'s callback only reports the CHANGED PATHS, not whether each was a
create/modify/delete -- so \code{FileChanged}'s \code{event} field is always
\code{"change"} here (documented downgrade; CC distinguishes change/add/unlink).
}
\keyword{internal}
