% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_interaction.R
\name{server_interaction}
\alias{server_interaction}
\title{Shiny interaction pause mechanism (Phase 3)}
\usage{
server_interaction(input, output, session, state)
}
\arguments{
\item{input, output, session}{Standard Shiny server args.}

\item{state}{The shared \code{reactiveValues} (must contain \code{pending_interaction}).}
}
\value{
A list with \code{ask_fn} and \code{ask_question_fn} (promise-returning).
}
\description{
Shared "pause -> wait for user -> resume" machinery for two
features that ride the same promise-as-pause-signal design:
\itemize{
\item \strong{ask_fn} -- harness permission approval (Allow/Deny a risky tool).
\item \strong{ask_question_fn} -- \code{AskUserQuestion} clarifying-question input.
}

Both store a single \code{state$pending_interaction} slot and expose an
interaction bar in the chat footer. The promise returned by the ask
functions is awaited by the (async) tool inside the streaming task; it is
resolved by the Allow/Deny/Submit observers here.

Hard-won constraints (see \code{inst/examples/test_shiny_ask_fn.R}):
\itemize{
\item The promise is ONLY a container for \code{resolve}; never use \code{then()} to do
UI side effects (then() runs with a NULL reactive domain).
\item All UI side effects happen inside the Allow/Deny/Submit observers, which
run in the correct reactive domain.
}
}
\keyword{internal}
