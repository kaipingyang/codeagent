% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_client.R
\name{r_mcp_server}
\alias{r_mcp_server}
\title{Create an R-based MCP server entry}
\usage{
r_mcp_server(
  tools_script = NULL,
  session_tools = FALSE,
  rscript = file.path(R.home("bin"), if (.Platform$OS.type == "windows") "Rscript.exe"
    else "Rscript")
)
}
\arguments{
\item{tools_script}{Character(1) or NULL. Path to an \code{.R} script that yields
a \code{list()} of \code{ellmer::tool()} objects.}

\item{session_tools}{Logical. Whether to expose built-in mcptools session
management tools. Default \code{FALSE}.}

\item{rscript}{Character(1). Path to the \code{Rscript} binary.}
}
\value{
A named list with \code{type}, \code{command}, and \code{args}.
}
\description{
Builds an \code{mcp_servers} list entry that launches an R subprocess running
\code{mcptools::mcp_server()} over stdio.
}
