% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_gate.R
\name{install_permission_gate}
\alias{install_permission_gate}
\title{Install codeagent's central permission gate on an existing Chat}
\usage{
install_permission_gate(
  chat,
  permission_mode = "default",
  rules = list(),
  tools = list(),
  ask_fn = NULL,
  tool_meta = list()
)
}
\arguments{
\item{chat}{An \code{ellmer::Chat}.}

\item{permission_mode}{Character. One of the 7 modes (default \code{"default"}).}

\item{rules}{List. Fine-grained permission rules (see \code{\link[=check_permission]{check_permission()}}).}

\item{tools}{List. \code{settings$tools} policy (\code{sets} / \code{capabilities} /
\code{overrides}).}

\item{ask_fn}{Function or NULL. Permission callback invoked when a tool needs
approval; may return a logical or a promise (async / Shiny). Called as
\code{ask_fn(name, input)}, or \verb{ask_fn(name, input, id = <tool_call_id>)} if it
declares an \code{id} argument or \code{...}.}

\item{tool_meta}{Named list mapping tool name -> capability
(\code{"read"}/\code{"write"}/\code{"exec"}/\code{"net"}), a convenience for calling
\code{\link[=register_tool_meta]{register_tool_meta()}} on each before installing the gate.}
}
\value{
Invisibly, \code{chat}.
}
\description{
For hosts that build a harness-only client
(\code{codeagent_client(register_tools = FALSE)}) and attach their own domain
tools, then want those tools governed by codeagent's central permission gate.

Declare each sensitive tool's capability with \code{\link[=register_tool_meta]{register_tool_meta()}} (or pass
\code{tool_meta} here), then call this once after attaching the tools. Tools whose
capability resolves to \code{"read"} are allowed without gating; \code{write}/\code{exec}/
\code{net} route through the gate under \code{permission_mode} + the \code{tools} policy.

Idempotent per chat: calling again refreshes the live mode / ask_fn / policy
rather than stacking a second gate.
}
\examples{
\dontrun{
client <- codeagent_client(chat, register_tools = FALSE)
chat$register_tool(my_write_tool)
install_permission_gate(
  chat, permission_mode = "default",
  tool_meta = list(MyWriteTool = "write"),
  ask_fn = function(name, input, id = NULL) host_prompt(id, name, input))
}
}
\seealso{
\code{\link[=register_tool_meta]{register_tool_meta()}}, \code{\link[=codeagent_client]{codeagent_client()}}
}
