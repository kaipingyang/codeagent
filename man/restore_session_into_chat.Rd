% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sessions.R
\name{restore_session_into_chat}
\alias{restore_session_into_chat}
\title{Restore a saved session's messages into a Chat object}
\usage{
restore_session_into_chat(chat, session_id = NULL, cwd = getwd())
}
\arguments{
\item{chat}{An \code{ellmer::Chat} to populate.}

\item{session_id}{Character. Session UUID. If \code{NULL}, the most recent
session under \code{cwd} is used (for \code{--continue}).}

\item{cwd}{Character. Project directory for session lookup.}
}
\value{
Invisibly, the resolved session id (or \code{NULL} if none found).
}
\description{
Loads a session and replays it into a Chat. Prefers the lossless
\code{chat-state} record (tool calls/results intact, via
\code{contents_record()}/\code{contents_replay()}); falls back to text-level turns for
pre-lossless sessions.
}
