% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_audit.R
\name{.audit_r_code_refs}
\alias{.audit_r_code_refs}
\title{Statically extract external-file references from R code.}
\usage{
.audit_r_code_refs(code)
}
\arguments{
\item{code}{Character (one or more lines) of R source. Untrusted -- parsed,
never evaluated.}
}
\value{
A list:
\itemize{
\item \code{static_paths} -- character vector of literal string paths passed to a
source/load-family call.
\item \code{dynamic} -- TRUE if the code uses a dynamic primitive, calls a
source-family function with no literal string argument (e.g.
\code{source(var)}), or assigns/references a source-family function as a bare
symbol (possible indirect call) -- i.e. a static pass cannot be sure what
it loads/runs.
\item \code{source_calls} -- which source-family functions were called (for audit).
\item \code{parse_error} -- TRUE if the code did not parse (treated as dynamic:
un-analysable).
}
}
\description{
Statically extract external-file references from R code.
}
\keyword{internal}
