% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{refresh_data_shield_context}
\alias{refresh_data_shield_context}
\title{Refresh the Data Shield schema block in a client's system prompt}
\usage{
refresh_data_shield_context(client)
}
\arguments{
\item{client}{A \code{CodeagentClient} (or a bare ellmer \code{Chat}, using default
settings/cwd).}
}
\value{
The \code{client}, invisibly.
}
\description{
Rebuilds the full system prompt (which includes each registered protected
dataset's filtered schema) and re-applies it to the client's Chat via
\code{set_system_prompt()}. Call this after registering or uploading data at
runtime (e.g. a Shiny \code{fileInput} handler) so the model sees the new
dataset's schema. Datasets registered before the client was built are already
in the initial system prompt and need no refresh.
}
\details{
\code{set_system_prompt()} replaces the prompt wholesale but preserves the
conversation history (turns), so the running chat is unaffected apart from a
one-time prompt-cache miss.
}
