% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{check_permission}
\alias{check_permission}
\title{Check whether a tool call is permitted}
\usage{
check_permission(
  tool_name,
  mode = "default",
  rules = list(),
  tool_input = NULL,
  allow_plan_exit = FALSE,
  capability = NULL
)
}
\arguments{
\item{tool_name}{Character(1). Name of the tool (e.g. \code{"Bash"}, \code{"Write"}).}

\item{mode}{Character(1). One of the values in \link{PermissionMode}.}

\item{rules}{List of \code{\link[=PermissionRule]{PermissionRule()}} objects. Every matching explicit deny
is absolute; declaration order is preserved only among remaining allow/ask
rules.}

\item{tool_input}{List or NULL. Tool arguments (used for Bash read-only detection).}

\item{allow_plan_exit}{Logical. Whether \code{ExitPlanMode} may restore a mode
after a trusted in-session \code{EnterPlanMode} transition.}

\item{capability}{Optional character(1). The caller's resolved capability for
this tool (\code{"read"}, \code{"write"}, \code{"exec"}, \code{"net"}). The central gate passes
what it resolved from the live \code{ToolDef}, which can see annotations a
name-only lookup cannot (btw sets \code{read_only_hint} on every tool it ships).
Ignored for tools listed in the built-in metadata, so a host cannot
downgrade \code{Bash} by passing \code{"read"}. \code{NULL} resolves from the tool name.}
}
\value{
Character(1): \code{"allow"}, \code{"deny"}, or \code{"ask"}.
}
\description{
Evaluates the permission decision for a single tool call, applying all
relevant rules in priority order.
}
