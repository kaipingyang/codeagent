% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_gate.R
\name{.input_gate_scan}
\alias{.input_gate_scan}
\title{Run the Data Shield input gate on a user turn's input.}
\usage{
.input_gate_scan(
  input,
  settings = list(),
  chat = NULL,
  on_progress = NULL,
  image_scanner = NULL
)
}
\arguments{
\item{input}{Character scalar OR a list whose first element is the typed text
and whose remaining elements are ellmer Content attachments.}

\item{settings}{The agent settings list (\code{data_shield_engine},
\code{data_shield_prompt_on_fail}, \code{data_shield_image_scanner}).}

\item{chat}{The ellmer Chat (fallback shield source via attribute).}

\item{on_progress}{Optional progress callback forwarded to \code{scan_prompt()}.}

\item{image_scanner}{Optional \verb{function(content_image) -> list(action, ...)}
for image attachments (default resolves from
\code{settings$data_shield_image_scanner}; \code{NULL} = images not scanned).}
}
\value{
A list: \code{action} (\code{"pass"}/\code{"redact"}/\code{"block"}), \code{input} (the
possibly-redacted input, same shape as the argument), and \code{text} (a message
when \code{action == "block"}). When no shield is active, returns
\code{list(action = "pass", input = input)}.
}
\description{
Call this in the agent loop / Shiny stream AFTER the UserPromptSubmit hook,
BEFORE the input is sent to the model. Handles both a bare character scalar
(CLI) and a contents list (Shiny: typed text + attachments).
}
\keyword{internal}
