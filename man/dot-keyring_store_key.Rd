% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keyring.R
\name{.keyring_store_key}
\alias{.keyring_store_key}
\title{Store an API key, preferring the OS credential store}
\usage{
.keyring_store_key(
  key_name,
  key_value,
  backend = c("auto", "keyring", "renviron")
)
}
\arguments{
\item{key_name}{Character. Environment variable name (e.g. \code{"OPENAI_API_KEY"}).}

\item{key_value}{Character. The secret value.}

\item{backend}{Character. \code{"auto"} (default) tries keyring then .Renviron;
\code{"keyring"} forces keyring (errors if unavailable);
\code{"renviron"} forces .Renviron.}
}
\value{
Invisibly \code{"keyring"} or \code{"renviron"} depending on which backend
was used.
}
\description{
Attempts to save \code{key_value} under \code{key_name} (service = "codeagent")
via \code{keyring}. If keyring is unavailable (no daemon, no package), falls
back to appending \code{KEY=value} to \verb{~/.Renviron} via \code{\link[=.append_renviron]{.append_renviron()}}.
}
\keyword{internal}
