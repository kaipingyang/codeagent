% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{codeagent_stream_async}
\alias{codeagent_stream_async}
\title{Stream one agent turn asynchronously}
\usage{
codeagent_stream_async(
  client,
  input,
  on_delta = NULL,
  on_thinking = NULL,
  on_tool_request = NULL,
  on_tool_result = NULL,
  on_error = NULL,
  on_usage = NULL,
  controller = NULL,
  tool_mode = "concurrent",
  session_id = NULL,
  iteration = 1L,
  cwd = NULL,
  compaction_ctrl = NULL,
  resource_state = NULL
)
}
\arguments{
\item{client}{A \code{CodeagentClient} (from \code{\link[=codeagent_client]{codeagent_client()}}) or a bare
\code{ellmer::Chat} object (or any list with a \verb{$stream_async} method for
testing via duck-typing).}

\item{input}{Character scalar. The user message.}

\item{on_delta}{Optional \verb{function(text_chunk)}. Called for each text chunk.}

\item{on_thinking}{Optional \verb{function(text)}. Called for thinking blocks
(only on models with extended thinking enabled).}

\item{on_tool_request}{Optional \verb{function(list(id, name, arguments, intent))}.
Called from the \code{ContentToolRequest} stream chunk, \strong{before} the
permission gate. Useful for displaying a "pending" tool card.}

\item{on_tool_result}{Optional \verb{function(list(id, name, display, value, is_error))}.
Called from the \code{ContentToolResult} stream chunk. \code{display} is a typed
toolcard contract from \link{tool_display} suitable for rich rendering.}

\item{on_error}{Optional \verb{function(message, recovered)}. Called on error.}

\item{on_usage}{Optional \verb{function(usage)}. Called at turn end with a list:
\code{n_tokens}, \code{model_limit}, \code{warning_state}, \code{cost_last} (USD or \code{NA}).}

\item{controller}{Optional \code{ellmer::stream_controller()} for cancellation.}

\item{tool_mode}{\code{"concurrent"} (default) or \code{"sequential"}. Passed to
\code{chat$stream_async(tool_mode=)}. Concurrent mode only accelerates
asynchronous tools; synchronous CLI tools execute serially regardless.}

\item{session_id}{Character or NULL. Passed to \code{\link[=save_session]{save_session()}}.}

\item{iteration}{Integer. Current turn iteration (affects system-reminder
injection and memory recall on iteration 1).}

\item{cwd}{Character or NULL. Working directory.}

\item{compaction_ctrl}{A \code{CompactionController} or NULL.}

\item{resource_state}{A \code{ContentReplacementState} or NULL.}
}
\value{
A \code{coro::async} promise resolving to
\code{list(text, usage, stop_reason)} where \code{stop_reason} is one of
\code{"completed"}, \code{"error"}, or \code{"interrupted"}.
}
\description{
Runs the full turn pipeline (compaction, system-reminder injection, session
save, cost tracking) and invokes typed callbacks for each content event.
}
\details{
\strong{Tool event dual paths} (see plan §6 for details):
\itemize{
\item \code{on_tool_request} / \code{on_tool_result} parameters are called from the
\code{ContentToolRequest} / \code{ContentToolResult} \strong{stream chunks}.
\code{on_tool_request} fires \strong{before} the permission gate ("pre-gate
notification"). \code{on_tool_result} receives a typed \code{display} contract from
\code{.adapt_tool_result()}.
\item \code{chat$on_tool_request} / \code{chat$on_tool_result} \strong{callbacks} (registered
by the permission gate, midloop compaction, and display helpers) are
independent and complementary.
}
}
\seealso{
\code{\link[=codeagent_stream]{codeagent_stream()}} for the synchronous wrapper.
}
