% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_shield.R
\name{shield_ingress}
\alias{shield_ingress}
\title{Configure universal tool-call ingress scanning}
\usage{
shield_ingress(
  langs = c("r", "python", "bash"),
  patterns = NULL,
  include_defaults = TRUE,
  on_fail = c("block", "ask"),
  ignore_case = TRUE
)
}
\arguments{
\item{langs}{Any of \code{"r"}, \code{"python"}, and \code{"bash"}; controls which
language-specific default rules are included.}

\item{patterns}{Optional named regular expressions using PCRE
(Perl-Compatible Regular Expression) syntax. A name matching a built-in
rule (e.g. \code{py_pandas_export}) replaces that rule; a new name is added.
Hosts wanting file-managed blacklists read their own file (e.g.
\code{yaml::read_yaml()}) into a named vector and pass it here.}

\item{include_defaults}{Include built-in serialization/encoding, network
transfer, shell data-file display, and protected-name preview rules.}

\item{on_fail}{\code{"block"} rejects the tool call; \code{"ask"} forces the existing
permission approval callback/UI (including the tool-call id).}

\item{ignore_case}{Apply case-insensitive matching.}
}
\value{
A Data Shield ingress strategy specification.
}
\description{
Scan every tool's arguments in the central permission gate before execution.
Default rules target high-confidence data serialization, encoded output,
network exfiltration, shell display of data files, and direct previews of
registered protected dataset names. This is defense-in-depth; egress remains
the primary model-data boundary.
}
\examples{
strict_calls <- shield_ingress(on_fail = "block")
supervised <- shield_ingress(langs = c("r", "python", "bash"), on_fail = "ask")
}
