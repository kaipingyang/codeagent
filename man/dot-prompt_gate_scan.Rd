% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt_gate.R
\name{.prompt_gate_scan}
\alias{.prompt_gate_scan}
\title{Run the Data Shield prompt gate on a user message.}
\usage{
.prompt_gate_scan(
  user_input,
  settings = list(),
  chat = NULL,
  on_progress = NULL
)
}
\arguments{
\item{user_input}{Character scalar. The raw user prompt (post-hook).}

\item{settings}{The agent settings list (for \code{data_shield_engine}).}

\item{chat}{The ellmer Chat (fallback shield source via attribute).}

\item{on_progress}{Optional progress callback forwarded to \code{scan_prompt()}.}
}
\value{
A list: \code{action} (\code{"pass"}/\code{"redact"}/\code{"block"}/\code{"ask"}) and \code{text}
(possibly redacted prompt). \code{"pass"}/\code{"redact"} mean "continue with \code{text}";
\code{"block"} means "reject this turn"; \code{"ask"} currently degrades to redact at
this call site unless the caller wires an approval path. When no shield is
active, returns \code{list(action = "pass", text = user_input)}.
}
\description{
Call this in the agent loop AFTER the UserPromptSubmit hook, BEFORE the
prompt is sent to the model. It resolves the active shield the same way the
tool gate does (settings engine first, then the chat attribute) and applies
\code{scan_prompt()}.
}
\keyword{internal}
