% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_ask_user.R
\name{ask_user_tool}
\alias{ask_user_tool}
\title{Create the AskUserQuestion tool}
\usage{
ask_user_tool(ask_question_fn = NULL, async = FALSE)
}
\arguments{
\item{ask_question_fn}{Function or NULL. If provided, called with
\verb{(question, choices)} instead of \code{readline()}. Used by the Shiny UI
to show an input bar and await the user's answer.}

\item{async}{Logical. When \code{TRUE}, the tool \code{fun} is a \code{coro::async}
function that \code{await()}s a promise returned by \code{ask_question_fn} (Shiny
path). Requires the chat to be run via \code{stream_async()}/\code{chat_async()}.
Leave \code{FALSE} for the synchronous CLI path (an async fun always returns a
promise, which the sync tool loop would not await).}
}
\value{
An \code{ellmer::ToolDef}.
}
\description{
Create the AskUserQuestion tool
}
