% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_gate.R
\name{input_gate}
\alias{input_gate}
\title{Input Gate --- the edge-1 counterpart of the tool gate}
\description{
The tool gate (\code{R/tools_gate.R}) guards edge 2 (tool traffic) by
wrapping ellmer's \code{on_tool_request} / tool functions. The \strong{input gate}
guards edge 1: everything the user sends \emph{before it reaches the model} ---
typed text AND attachments (text-bearing content, images). Unlike the tool
gate it has no ellmer chat-level hook to attach to (user input is handled
inside \code{agent_loop} / the Shiny stream, not inside the Chat object), so the
input gate is a function those entry points call at the UserPromptSubmit
point.

Two consumers run at this edge, in order (mirroring the tool gate's
hooks-then-shield layering), kept as SEPARATE systems:
\enumerate{
\item \strong{hooks} \code{run_user_prompt_submit()} -- aligns with Claude Code's
\code{UserPromptSubmit}; may \code{block} or append context, NEVER redacts
(handled in \code{agent_loop} directly).
\item \strong{Data Shield} \code{scan_prompt()} -- the shield's own confidentiality
scan; MAY redact (protected values / PII the user pasted in). This file
wires that second consumer.
}

Input types and how each is handled (see \code{.input_gate_scan}):
\itemize{
\item \strong{text} (typed message)        -> \code{scan_prompt}; redact/block/ask.
\item \strong{text-bearing attachment}     -> extract text, scan; cannot be redacted
in place (Content is immutable) so a
hit fails safe to \strong{block}.
\item \strong{image attachment}            -> optional \code{image_scanner} hook
(default \code{NULL} = blind spot, passed
through). Host may inject a scanner
(OCR / VLM) returning a decision.
}
}
\keyword{internal}
